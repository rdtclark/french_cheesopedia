# French Cheesopedia

This simple Vue project accesses an API to retrieve a list of all French cheeses.

These are then displayed to the user through an interactive map, and a colourized list
based on cheese types.

## Project setup

Clone the repo

```
npm install
npm run serve
```
